# Images

- `logo.svg` — PB monogram, uses `currentColor` so it adapts to light/dark theme
- `favicon.svg` — Dark square variant for browser favicon
- `og-default.jpg` — Default Open Graph image (1200×630). Add before launch.
- `showcase-ftt.jpg` — The Freethinking Times screenshot (16:10). Add before launch.
